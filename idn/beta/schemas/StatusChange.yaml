type: object
properties:
  previousStatus:
    type: string
    description: previous status of the account (LOCKED, DISABLED or ENABLED)
  newStatus:
    type: string
    description: new status of the account (LOCKED, DISABLED or ENABLED)